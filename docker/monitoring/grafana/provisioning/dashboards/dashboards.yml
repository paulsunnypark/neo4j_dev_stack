apiVersion: 1

providers:
  - name: Default
    folder: Neo4j Dev Stack
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
